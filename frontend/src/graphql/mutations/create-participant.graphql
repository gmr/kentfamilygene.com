mutation CreateParticipant($input: CreateParticipantInput!) {
  createParticipant(input: $input) {
    id
    displayName
    email
    membershipType
    isActive
    ftdnaKitNumber
    joinDate
    contactNote
    researchGoal
    createdDate
    updatedDate
  }
}
