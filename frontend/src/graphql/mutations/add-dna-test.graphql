mutation AddDnaTest($participantId: String!, $input: CreateDnaTestInput!) {
  addDnaTest(participantId: $participantId, input: $input) {
    id
    testType
    testName
    provider
    kitNumber
    markerCount
    registeredWithProject
    gedmatchKit
  }
}
