query Haplogroups($haplogroupType: String) {
  haplogroups(haplogroupType: $haplogroupType) {
    items {
      id
      name
      subclade
      abbreviation
      confirmationStatus
      haplogroupType
    }
    total
    hasMore
  }
}
