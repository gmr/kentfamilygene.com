query AdminNotes($color: String, $resolved: Boolean) {
  adminNotes(color: $color, resolved: $resolved) {
    items {
      id
      color
      text
      createdDate
      resolved
    }
    total
    hasMore
  }
}
