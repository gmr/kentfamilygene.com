query Places($country: String) {
  places(country: $country) {
    items {
      id
      name
      county
      state
      country
      lat
      lon
      familysearchUrl
    }
    total
    hasMore
  }
}
