query AdminParticipant($id: String!) {
  adminParticipant(id: $id) {
    id
    displayName
    email
    membershipType
    isActive
    ftdnaKitNumber
    joinDate
    contactNote
    researchGoal
    createdDate
    updatedDate
  }
}
