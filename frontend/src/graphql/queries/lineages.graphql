query Lineages($region: String) {
  lineages(region: $region) {
    items {
      id
      originState
      lineageNumber
      displayName
      region
      statusNote
      isNew
      newLineageDate
      createdDate
      updatedDate
    }
    total
    hasMore
  }
}
